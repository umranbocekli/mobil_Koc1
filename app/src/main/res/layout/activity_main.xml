<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Alt kenara hizala -->

    <Button
        android:id="@+id/buttonBox1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="44dp"
        android:text="takvim"
        app:layout_constraintBottom_toBottomOf="@+id/buttonBox2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/buttonBox2"
        app:layout_constraintVertical_bias="0.0" />

    <Button
        android:id="@+id/buttonBox2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="72dp"
        android:text="Günlük"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.788"
        app:layout_constraintStart_toEndOf="@+id/buttonBox1"
        app:layout_constraintTop_toTopOf="parent" />  <!-- Alt kenara hizala -->

</androidx.constraintlayout.widget.ConstraintLayout>
